// Importing the 'Location' and 'User' models
const Location = require("./Location");
const User = require("./User");

// Exporting the 'User' and 'Location' models as an object
module.exports = { User, Location };
